## Example tests with some simple Mono/Flux

Remember that all the publisher events must be accumulated into a
single serializable test result.

Run tests with 
```bash
./gradlew<.bat> clean test --info
```